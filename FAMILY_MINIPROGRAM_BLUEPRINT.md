# 小家菜馆子家庭版微信小程序蓝图

## 1. 项目目标

为家庭堂食设计一个轻量点餐系统。使用者无需登录即可浏览菜单、提交订单和查看当前订单进度；注册登录是可选功能。管理者可以维护菜单并更新订单进度。

第一阶段不接入真实支付、不支持外卖、不支持多门店，也不面向公开商用。

## 2. 产品组成

| 部分 | 使用者 | 作用 |
| --- | --- | --- |
| 统一小程序 | 游客、已注册用户、管理者 | 所有人从同一入口进入；根据账号权限显示点餐或管理页面 |
| 微信云开发 | 系统内部 | 保存数据、处理登录和订单操作 |

第一阶段只维护一个小程序项目，不单独开发商家小程序。游客默认看到点餐页面；管理者登录后才会在“我的”页面看到菜单管理和订单管理入口。以后需要时，可以把管理页面独立为网页后台，而不改变订单数据。

## 3. 登录设计

小程序默认以游客模式打开，不显示强制登录页面。游客可以浏览菜单、加入购物车、提交订单，并在当前微信和当前设备上查看自己的订单进度。

注册登录是可选功能。登录后可以长期保存历史订单、收藏菜品和个人资料，并在更换设备后继续访问这些数据。

初版支持以下两种登录标识：

- 账号 + 密码
- 手机号 + 密码

手机号在第一阶段只作为登录名使用，不发送短信验证码。短信验证需要接入第三方短信服务，会增加费用和配置工作，家庭版暂不需要。游客无需填写手机号或账号。

账号密码不能明文保存。云函数只保存经过加密处理后的密码信息，管理者也无法直接查看原密码。

账号角色分为：

| 角色 | 权限 |
| --- | --- |
| 点餐用户 | 浏览菜单、创建和查看自己的订单 |
| 管理者 | 管理菜单、分类和全部订单 |

管理者身份由系统数据设置，不在点餐页面提供随意切换入口。

## 4. 页面清单

### 点餐用户

1. 首页菜单：默认入口，分类、搜索、菜品图片、价格、售罄状态。
2. 菜品详情：描述、口味选择、数量。
3. 购物车：已选菜品、数量、备注、订单总额。
4. 提交订单：确认菜品和备注，不进行支付。
5. 我的订单：游客查看当前设备的订单；已登录用户查看长期历史订单。
6. 我的：提供登录与注册入口；登录后显示头像、昵称和退出登录。

### 管理者

1. 管理首页：待处理订单数量、今日订单数量。
2. 订单管理：查看订单菜品和备注，更新订单状态。
3. 菜品管理：新增、编辑、删除、上架、售罄。
4. 分类管理：新增和修改菜品分类。

优惠券、收藏、数据报表保留为后续可选功能，不放进第一阶段的必做范围。

### 后续数据维护

- 订单默认长期保留，当前阶段不自动删除任何订单。
- 家庭使用稳定后，可增加订单归档功能：保留最近 180 天的订单，较早订单先导出备份，再由管理员确认删除。
- 归档与删除必须由云端定时任务和管理员确认共同控制，避免误删历史记录。

### 待确认的库存体验优化

- 顾客端加购达到菜品库存上限时，将加号变灰并限制继续加购；购物车中的加号同步限制。
- 顾客端不展示具体剩余库存数字，只在库存归零时显示“已售罄”。
- 顾客首次加到库存上限时提示“已达可购上限”，之后灰色加号不再响应，避免重复弹窗。
- 前端限制只用于改善体验；提交订单时云函数仍需在事务中再次校验库存，确保多人同时下单时不会超卖。
- 库存判断逻辑应抽成点菜页和购物车页共用的小工具，避免在多个页面重复编写后产生不一致。
- 此项默认不执行。在后续继续开发库存功能、每日重置或多人点餐前，必须先提醒用户再次确认后才能实施。

### 待确认的结算体验优化

- 在点菜页结算悬浮栏中增加“清空购物车”功能，用于一次移除全部已选菜品。
- 此项默认不执行。在后续修改结算悬浮栏时，必须先提醒用户再次确认后才能实施。

## 5. 订单流程

堂食家庭版不需要桌号和取餐码。订单状态固定为：

```text
已提交 -> 制作中 -> 待上菜 -> 已完成
             -> 已取消
```

只有管理者可以把订单推进到下一个状态或取消订单。游客和点餐用户只能查看归属自己的订单进度。

## 6. 核心数据

微信云开发使用“集合”保存数据，可以把它理解成数据库中的不同表。

| 集合 | 保存内容 |
| --- | --- |
| users | 已注册用户的账号、手机号、密码加密信息、昵称、头像、角色 |
| categories | 分类名称、图标、排序号 |
| dishes | 菜品名称、价格、图片、描述、分类、是否售罄、是否上架 |
| orders | 订单归属标识、订单号、菜品明细、备注、总金额、订单状态、创建时间 |

订单明细会保存下单当时的菜品名称和价格。即使之后修改了菜品价格，旧订单也不会改变。游客订单会使用当前微信和设备生成的匿名归属标识，不能查看其他人的订单；清理小程序数据或更换设备后，游客历史记录可能无法找回。

## 7. 技术边界

第一阶段使用微信云开发，不直接使用本机 MySQL。原因是受邀使用者的手机不能安全地直接访问你电脑里的 MySQL；微信云开发可以先省去服务器、域名和网络配置。

电脑上的 MySQL 和 Navicat 保留即可。未来项目需要更多人使用、需要独立网页商家后台或数据更复杂时，可以把云端数据迁移到 MySQL。

## 8. 开发阶段

### 阶段一：小程序基础

- 创建微信小程序项目和云开发环境。
- 建立菜单、分类、订单的基础数据结构。
- 完成登录注册和管理者角色。

### 阶段二：点餐流程

- 完成菜单浏览、菜品详情、购物车和提交订单。
- 完成用户订单列表与订单状态展示。

### 阶段三：管理功能

- 完成菜品和分类管理。
- 完成订单状态处理。

### 阶段四：家庭试用

- 邀请家人使用微信体验版测试。
- 根据实际使用情况调整菜单和流程。

## 9. 你需要准备的内容

- 菜品名称、价格、分类和图片。
- 用于管理者的账号名称；密码请在实际注册时自行输入，不要发到聊天中。
- 家庭试用者不需要提前注册账号或提供手机号，可以直接使用游客模式。
- 微信开发者账号。创建微信云开发环境前再准备即可。

## 10. 下一项需要确认的操作

下一步会创建微信小程序项目和微信云开发环境。这会在微信平台创建外部资源，需要先由你确认并登录自己的微信开发者账号。创建前会单独说明具体步骤，不会自动操作你的账号。
