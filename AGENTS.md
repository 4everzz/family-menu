# 项目协作规则

1. 用户是编程新手。遇到专业术语、操作或步骤时，使用通俗中文解释。
2. 执行重要决定或任务前，先用中文说明方案并等待用户批准。
3. 代码中的注释必须使用中文。
4. 开发前端、后端、数据库、云开发或部署功能时，说明所使用的框架、专业术语及其在企业招聘面试中的常见作用；用户询问不熟悉的内容时，先用通俗语言解释，再补充专业表述。
5. 每次准备修改功能前，先评估现有代码与拟议方案是否会造成重复逻辑、难维护结构或引发新 bug。若存在明显风险，必须先用中文说明原因、影响和更稳妥的做法，等待用户同意后再实施；若风险较低，也要简要说明评估结论。每个独立功能完成后必须先检查和测试，汇报结果并等待用户确认后再继续下一段。
6. 每次完成代码修改、功能开发、重构或重要优化后，默认把用户视为正在学习并准备面试的项目参与者，不能只说明“已完成”。交付说明应按以下结构逐项解释：
   - 这次改动解决了什么问题：先使用通俗中文说明。
   - 用了什么框架或技术：说明当前项目实际使用的框架、技术、接口或设计思路；出现术语时先解释通俗含义，再补充专业名称。
   - 为什么这样设计：说明替代方案、维护性、安全性或性能方面的原因。
   - 企业项目归类：明确属于前端、后端、数据库、云开发、权限控制或架构设计中的哪一类。
   - 面试官可能怎么问：结合当前改动给出适合初级或实习面试的提问与回答思路；涉及多个知识点时分开解释。
   - 我这次应该记住什么：在检查和测试结果后，明确给出本次应掌握的知识点和实践结论。
   - 简历价值：若改动属于项目亮点，明确说明是否可写入简历及推荐表述；不属于亮点时也要明确说明。
7. 小程序端只保留顾客点餐和店铺现场管理能力；平台超级管理员专属能力（跨店建店、跨店授权、全局用户管理、跨店报表等）默认放到 Web 超级管理员后台，不再挤占小程序“我的”页或店铺管理页布局。
8. 微信小程序原生 `button` 自带默认样式，放入 flex/grid/双列按钮区域时必须显式设置 `width/max-width/min-width/box-sizing/margin/padding/line-height`，并处理 `button::after` 边框，禁止裸用导致按钮溢出、换行或挤压。
9. 回答风格按任务复杂度区分：简单事实问答、短解释和普通聊天保持自然简洁，不默认启用 ADHD 任务格式；涉及重要代码修改、架构设计、部署、排错、测试、学习规划或三个及以上步骤的操作时，默认采用 ADHD 风格，明确当前状态、下一步动作、步骤顺序、预计耗时与完成结果，避免一次堆出过多待办。
10. 图片识别：当用户发送图片路径或图片链接并要求识别、描述、分析图片内容时，不要用 Read 工具直接读图片，改用项目根目录的 `vision.js` 识图脚本，运行：
    node --env-file=.env vision.js "<图片路径>" "请用中文描述这张图片的内容"
    如果是网络图片链接，则加 `--url` 参数：
    node --env-file=.env vision.js --url "<图片链接>" "请用中文描述这张图片的内容"
    每张图片运行一次，拿到描述后再回答用户。脚本配置在 `.env`（已加入 .gitignore，不要提交到 Git）。
