"""MCP（Model Context Protocol）集成。

本项目用 MCP 做两件事，两侧各一个子包：

  · **客户端**（本包的 `gateway` + `servers`）——
    连外部 MCP Server。当前接的是 `cn-food-mcp`（中国食物成分表），
    用来把"热量"从"大模型估的"变成"查出来的"。

  · **服务端**（`server/` 子包 + 根目录 run_mcp_server.py）——
    把本项目「查冰箱 / 查菜单」这些只读能力暴露成 MCP 工具，
    供 Claude Desktop、Cursor 这类外部 AI 客户端调用。

⭐ 为什么值得用 MCP（面试要点）
   判断标准只有一条：**这个能力是不是来自项目之外、且未来可能被替换。**

   食材营养数据完美符合——它在项目之外，且将来可能换成别的数据源、
   或换成自建服务。抽象成 MCP 之后，业务代码只认 `search_food` 这个工具名，
   底下换成谁都不用改一行。

   ⚠️ 反例（也重要）：查自己库里的菜单，**不该**套 MCP。
   直接调 service 就行——自己的内部查询套一层 MCP 只是增加延迟和故障点。
   "知道什么时候不用"比"会用"更能体现工程判断力。
"""
