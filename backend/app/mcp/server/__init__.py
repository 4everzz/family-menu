"""MCP Server 子包：把本项目的能力按 MCP 协议暴露给外部 AI 客户端。

和同级的 `gateway`（客户端）方向正好相反：

    客户端 gateway/    本项目 → 调别人的 MCP Server（查食物热量）
    服务端 server/     别人的 AI 客户端 → 调我们的工具（查冰箱、查菜单）

两个方向的代码放在一个 `app/mcp` 包里，是因为它们共享同一套东西：
`settings` 里的开关、对协议的理解、以及"哪些该走 MCP、哪些不该走"的判断。
但**不要互相 import**——客户端和服务端没有依赖关系，
混在一起会让"只想用客户端"的部署多装一堆服务端依赖。

⚠️ 这里的工具全部是**只读**的。为什么不做写操作（加食材、改菜谱）？
    写操作经过 AI 转述会丢失上下文——用户说"把冰箱里那个快过期的清掉"，
    模型挑中的是哪个"那个"、清成什么样，中间任何一步理解偏差都会造成真实数据丢失，
    而且不可撤销。只读工具最坏的结果是"查错了"，用户一眼能看出来。
    要放开写操作，前提是先做出确认机制（比如返回待确认草稿让用户在 App 里点一下），
    那是另一个话题，不在当前范围内。
"""
